import { TextToIconPipe } from './textToIcon.pipe';

describe('TextToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TextToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
