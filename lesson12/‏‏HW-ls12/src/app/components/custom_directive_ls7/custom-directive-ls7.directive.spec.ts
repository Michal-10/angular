import { ElementRef } from '@angular/core';
import { CustomDirectiveLs7Directive } from './custom-directive-ls7.directive';


describe('CustomDirectiveLs7Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectiveLs7Directive();
    expect(directive).toBeTruthy();
  });
});
